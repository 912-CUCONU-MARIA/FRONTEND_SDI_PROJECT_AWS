import { PlayerCharacterItemDto } from './player-character-item-dto';

describe('PlayerCharacterItemDto', () => {
  it('should create an instance', () => {
    expect(new PlayerCharacterItemDto()).toBeTruthy();
  });
});
