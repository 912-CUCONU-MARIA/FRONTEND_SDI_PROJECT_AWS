import { PlayerCharacterItemNameDto } from './player-character-item-name-dto';

describe('PlayerCharacterItemNameDto', () => {
  it('should create an instance', () => {
    expect(new PlayerCharacterItemNameDto()).toBeTruthy();
  });
});
